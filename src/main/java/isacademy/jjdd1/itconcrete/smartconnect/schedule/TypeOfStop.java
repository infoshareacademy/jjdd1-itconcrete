package isacademy.jjdd1.itconcrete.smartconnect.schedule;

/**
 * Created by Ageee on 05.04.2017.
 */
public enum TypeOfStop {
    FIRST_STOP, INTERMEDIATE_STOP, LAST_STOP

    /*public static TypeOfStop fromString(String stopLabel) {
        try {
            return TypeOfStop.valueOf(stopLabel.toUpperCase());
        } catch (IllegalArgumentException e) {
            return null;
        }
    }*/
}
